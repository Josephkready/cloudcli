# Keep the docker build context lean. The builder stage does a full `npm ci`
# + `npm run build` from source, so any committed/local build artefacts and
# installed deps must NOT be copied in (they'd be stale and bloat the context).
.git
node_modules
dist
dist-server
*.log
npm-debug.log*
.env
.env.*
